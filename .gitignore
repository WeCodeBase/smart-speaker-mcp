# ── Compiled binaries (root only — anchored with leading slash so they
#    do NOT match the cmd/smart-speaker-mcp/ source directory) ───────────────
/smart-speaker-mcp
/smart-speaker-mcp.exe
/smart-speaker-mcp-darwin-amd64
/smart-speaker-mcp-darwin-arm64
/smart-speaker-mcp-linux-amd64
/smart-speaker-mcp-linux-arm64
/smart-speaker-mcp-windows-amd64.exe
/google-home-mcp
/google-home-mcp.exe

# ── Build output ──────────────────────────────────────────────────────────────
dist/
*.o
*.a

# ── Secrets — NEVER commit these ─────────────────────────────────────────────
.env
.env.local
.env.*.local
.mcpregistry_github_token
.mcpregistry_registry_token
*.token
*.pem
*.key
*_secret*
*_credentials*

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── VS Code ───────────────────────────────────────────────────────────────────
.vscode/*.backup

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.idea/
*.swp
*.swo
*~
